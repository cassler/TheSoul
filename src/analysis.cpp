#include "analysis.hpp"

// This translation unit is intentionally left blank. The analysis module is
// implemented inline in the header so it can be shared across multiple tools
// without introducing duplicate symbol definitions. This file remains only to
// keep legacy build scripts that expect it in place.
